language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wojo-frontend' ]; then cd .. && eval "mv $currentfolder generator-wojo-frontend" && cd generator-wojo-frontend; fi

